# Build stage
FROM golang:1.21-alpine AS builder
WORKDIR /app
COPY main.go .
RUN go build -o htmx-frontend main.go

# Runtime stage
FROM alpine:3.19
WORKDIR /app
COPY --from=builder /app/htmx-frontend .
EXPOSE 8080
CMD ["./htmx-frontend"]
